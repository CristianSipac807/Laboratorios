#include "notas_facultad.h" // Incluye el archivo de encabezado notas_matriz.h
/*
Laboratorio sobre librerias, utilizando la base del codigo de matrices que se utilizo el codigo anterior, realizando cambios
unicamente en el orden del codigo, ya que se utilizo el mismo codigo.
*/
int main() {
    srand(getpid());
    llamaCiclo();
    return 0;
}

